The savepaths of the generated images and labelIds should follow the corresponding paths in the file validation.odgt(which is in data directory)

The pretrained models are in the directory ckpt/ade20k-resnet101-upernet/

